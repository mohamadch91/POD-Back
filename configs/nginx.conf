http {
    upstream authentication {
        server authentication:8001;
    }

    upstream baseInfo {
        server baseInfo:8002;
    }

    server {
        listen 80;

        location /auth/ {
            proxy_pass http://authentication;
        }

        location /service2/ {
            proxy_pass http://baseInfo;
        }
    }
}

events {}